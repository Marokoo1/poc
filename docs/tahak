# Git tahák pro projekt `poc`

## Začátek práce

```bash
git pull origin main
git status
Uložení změn
Všechny změny
git add .
git commit -m "Popis zmen"
git push origin main
Jen konkrétní soubor
git add src/poc_backtest.py
git commit -m "Update poc_backtest.py"
git push origin main
Kontrola změn
Stav repa
git status
Rozdíly
git diff
Rozdíly jen v jednom souboru
git diff src/poc_backtest.py
Krátká historie
git log --oneline -n 10
Když chceš zahodit změny
Jeden soubor
git restore src/poc_backtest.py
Všechny necommitnuté změny
git restore .
Když git pull nejde kvůli lokálním změnám
git stash
git pull origin main
git stash pop
Doporučený pracovní rytmus
Když začínáš
git pull origin main
git status
Když končíš
git status
git add .
git commit -m "Update project"
git push origin main
Praktická poznámka pro tento projekt

Před větší změnou v backtestu nebo dashboardu je rozumné si uložit zálohu:

cp src/poc_backtest.py src/poc_backtest.py.bak
cp src/poc_backtest_dashboard.py src/poc_backtest_dashboard.py.bak
