import pandas as pd


def calculate_poc(df: pd.DataFrame) -> pd.DataFrame:
    """
    Placeholder pro budoucí výpočet POC.
    Zatím jen vrátí stejný dataframe.
    """
    if df.empty:
        return df

    result = df.copy()
    return result
